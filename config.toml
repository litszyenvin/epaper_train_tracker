# Example config.toml for epaper_train_disp_w_button.py (moved to repo root)

# Default periodic refresh when active (seconds)
INTERVAL_SECONDS_DEFAULT = 300

# Faster during the key morning window (seconds)
INTERVAL_SECONDS_MORNING = 120

# Sleep window: pause auto refresh, show sleeping msg
# Use arrays for hour/minute tuples
SLEEP_START_HM = [21, 0]
SLEEP_END_HM = [5, 0]

# Morning behaviour (local time on the Pi)
MORNING_WINDOW_START_HM = [7, 0]
MORNING_WINDOW_END_HM = [8, 45]
TARGET_DEPART_START_HM = [7, 45]
TARGET_DEPART_END_HM = [8, 45]
FAST_MAX_MINUTES = 30
MORNING_FETCH_COUNT = 20

# Manual wake display duration during sleep window (seconds)
MANUAL_DISPLAY_SECONDS = 900

# Full clear cadence to reduce ghosting (1 full clear every N updates)
FULL_REFRESH_EVERY = 12

# Trains to show on screen
NUMBER_OF_TRAINS = 4

# RTT settings
url_head = "https://api.rtt.io/api/v1/json/search/"
ORIGIN = "SAC"
DESTINATION = "ZFD"
username = "rttapi_litszyenvin"
# For security, prefer setting the password via env var or leave blank here
password = "bec5d38d598f2a3518962fedf8345569696cb0bf"
